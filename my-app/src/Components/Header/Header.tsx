import React from 'react';
import "./Header.css";

const Header = () => {
    return (
        <div className="header">
            <h2 className="header__brand">ResumersPrototype</h2>
        </div>
    );
};

export default Header;